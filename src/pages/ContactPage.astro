---
import Layout from "../layouts/Layout.astro";
import LandingHeader from "../components/LandingHeader.astro";
const FORM_ACTION = "https://formspree.io/f/your-form-id"; // <-- cambia por tu endpoint
---

<Layout title="Contáctanos – Fix Group" description="Asesoría comercial y soporte. Escríbenos para cotizaciones, demos y atención inmediata.">
  <LandingHeader />

  <!-- HERO -->
  <section class="relative overflow-hidden bg-black">
    <img src="/images/contact-hero.jpg" alt="" class="absolute inset-0 w-full h-full object-cover opacity-25" />
    <div class="absolute inset-0 bg-[radial-gradient(60%_60%_at_80%_20%,rgba(16,185,129,0.15),transparent)]"></div>
    <div class="relative max-w-7xl mx-auto px-6 md:px-8 py-20 md:py-28">
      <p class="uppercase tracking-[0.25em] text-xs text-white/70">Estamos para ayudarte</p>
      <h1 class="mt-3 text-4xl md:text-6xl font-extrabold text-white">Contáctanos</h1>
      <p class="mt-4 text-white/80 max-w-2xl text-lg">
        Cotizaciones, soporte y demos. Respuesta en menos de 24&nbsp;horas.
      </p>
    </div>
  </section>

  <!-- CONTENIDO -->
  <main class="bg-black text-white">
    <section class="max-w-7xl mx-auto px-6 md:px-8 py-12 md:py-16 grid grid-cols-1 lg:grid-cols-2 gap-10">

      <!-- FORMULARIO -->
      <div class="rounded-3xl bg-white/5 border border-white/10 backdrop-blur p-6 md:p-8 shadow-2xl">
        <h2 class="text-2xl font-semibold">Escríbenos</h2>
        <p class="text-white/70 mt-1">Cuéntanos qué necesitas y un asesor te contactará.</p>

        <form action={FORM_ACTION} method="POST" class="mt-6 grid grid-cols-1 md:grid-cols-2 gap-4">
          <div class="md:col-span-1">
            <label class="text-sm text-white/70">Nombre</label>
            <input required name="nombre" class="mt-1 w-full rounded-xl bg-white/10 border border-white/10 p-3 text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-emerald-400/60" placeholder="Tu nombre" />
          </div>
          <div class="md:col-span-1">
            <label class="text-sm text-white/70">Empresa</label>
            <input name="empresa" class="mt-1 w-full rounded-xl bg-white/10 border border-white/10 p-3 text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-emerald-400/60" placeholder="Nombre de empresa" />
          </div>
          <div class="md:col-span-1">
            <label class="text-sm text-white/70">Email</label>
            <input required type="email" name="email" class="mt-1 w-full rounded-xl bg-white/10 border border-white/10 p-3 text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-emerald-400/60" placeholder="correo@dominio.com" />
          </div>
          <div class="md:col-span-1">
            <label class="text-sm text-white/70">Teléfono</label>
            <input name="telefono" class="mt-1 w-full rounded-xl bg-white/10 border border-white/10 p-3 text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-emerald-400/60" placeholder="+593 9..." />
          </div>
          <div class="md:col-span-2">
            <label class="text-sm text-white/70">Asunto</label>
            <select name="asunto" class="mt-1 w-full rounded-xl bg-white/10 border border-white/10 p-3 text-white focus:outline-none focus:ring-2 focus:ring-emerald-400/60">
              <option value="cotizacion">Cotización</option>
              <option value="demo">Solicitar demo</option>
              <option value="soporte">Soporte</option>
              <option value="otro">Otro</option>
            </select>
          </div>
          <div class="md:col-span-2">
            <label class="text-sm text-white/70">Mensaje</label>
            <textarea required name="mensaje" rows={5} class="mt-1 w-full rounded-xl bg-white/10 border border-white/10 p-3 text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-emerald-400/60" placeholder="Cuéntanos brevemente tu caso"></textarea>
          </div>

          <input type="hidden" name="_subject" value="Contacto web – Fix Group" />
          <input type="text" name="_gotcha" class="hidden" />

          <div class="md:col-span-2 flex flex-wrap items-center gap-4">
            <button class="rounded-full bg-emerald-400 text-gray-900 px-7 py-3 font-semibold hover:bg-emerald-500 transition" type="submit">
              Enviar solicitud
            </button>
            <p class="text-xs text-white/60">Al enviar aceptas nuestros términos y política de privacidad.</p>
          </div>
        </form>
      </div>

      <!-- INFO + MAPA -->
      <div class="space-y-6">
        <!-- Tarjetas glass -->
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-6">
          <div class="rounded-2xl bg-white/5 border border-white/10 p-6">
            <div class="text-sm uppercase tracking-widest text-white/60">Email</div>
            <a href="mailto:info@fixgroup.net" class="mt-1 block text-lg font-semibold hover:underline">info@fixgroup.net</a>
          </div>

          <div class="rounded-2xl bg-white/5 border border-white/10 p-6">
            <div class="text-sm uppercase tracking-widest text-white/60">Llámanos</div>
            <div class="mt-1 space-y-1">
              <a href="tel:+593996206592" class="block hover:underline">+593 9 9620 6592</a>
              <a href="tel:+593022465632" class="block hover:underline">0 22 465 632</a>
            </div>
          </div>

          <div class="rounded-2xl bg-white/5 border border-white/10 p-6 sm:col-span-2">
            <div class="text-sm uppercase tracking-widest text-white/60">Dirección</div>
            <p class="mt-1 text-white/90">Francisco Cruz Miranda 17-18 y Mañosca, Quito</p>
            <p class="text-white/60 text-sm">Ecuador</p>
          </div>

          <div class="rounded-2xl bg-white/5 border border-white/10 p-6 sm:col-span-2">
            <div class="text-sm uppercase tracking-widest text-white/60">Horarios</div>
            <p class="mt-1 text-white/90">Lunes a Viernes: 09:00–18:00</p>
            <p class="text-white/60 text-sm">Soporte 24/7 para clientes con SLA</p>
          </div>
        </div>

        <!-- Mapa -->
        <div class="rounded-3xl overflow-hidden border border-white/10 bg-white/5">
          <iframe
            src="https://www.google.com/maps?q=Fix%20Group%20Quito%20Ecuador&output=embed"
            class="w-full h-[320px]"
            loading="lazy"
          ></iframe>
        </div>


    <!-- CTA final -->
    <section class="py-12">
      <div class="max-w-7xl mx-auto px-6 md:px-8">
        <div class="rounded-3xl bg-gradient-to-r from-emerald-500/15 to-white/5 border border-white/10 p-8 md:p-12 flex flex-col md:flex-row items-center justify-between gap-6">
          <div>
            <h3 class="text-2xl font-bold">¿Prefieres WhatsApp?</h3>
            <p class="text-white/80">Escríbenos y agenda una demo de SUNMI o MiWiFi.</p>
          </div>
          <a href="https://wa.me/593996206592?text=Hola%20Fix%20Group%2C%20quiero%20informaci%C3%B3n"
             class="rounded-full border border-white/40 px-7 py-3 hover:bg-white hover:text-black transition">
            Abrir WhatsApp
          </a>
        </div>
      </div>
    </section>
  </main>
</Layout>
